setup:
  addons:
    - plan: jawsdb-maria
      as: DATABASE
build:
  docker:
    web: rails_app/Dockerfile
  config:
    # RAILS_ENV: development
    RAILS_ENV: production
release:
  command:
    - rails db:create
    # - ./_install.sh
  image: web
run:
  web: bundle exec puma -C config/puma.rb
